const Header = () => {
  return (
    <div
      style={{
        textAlign: "center",
        fontWeight: "bold",
        fontSize: "24px",
        marginBottom: "20px",
      }}
    >
      Heap Sort Visualization (080BCT061)
    </div>
  );
};

export default Header;
